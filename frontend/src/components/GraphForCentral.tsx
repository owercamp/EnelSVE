import React, { FC } from 'react'
import { IGraphInform } from '../../interfaces/interfaces'

const GraphForCentral: FC<IGraphInform> = ({ }) => {
  return (
    <div>GraphForCentral</div>
  )
}

export default GraphForCentral